// exercise7.js

function allCapps(str) {
  if (str.length > 10) {
    return str.toUpperCase();
  } else {
      return str
    }
}

console.log(allCapps('happy doodle'));
console.log(allCapps('doodle'));
