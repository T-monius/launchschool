# daily_double_notes.txt

Undertanding
  - Input
    + String
    + May have duplicate consecutive characters
  - Task
    + Remove duplicate consecutive characters from string
    + Cannot use pre-written Ruby library method `String#squeeze(!)`
  - Output
    + New String

Implementation
  - Define `crunch` method
    + Return immediately if the string is empty
    + Initialize an empty return string
    + Iterate through original string with an index
      > Compare current character to the previous
      > If current character is not equal to the previous push
        it to the return string || if the return string is empty
