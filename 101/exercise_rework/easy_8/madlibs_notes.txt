# madlibs_notes.txt

Understanding
  - Story template
    + Blanks for words
  - List of words
    + noun, verb, adjective, adverb
    + Prompt user for each word type
  - Input
    + Four word types
  - Task
    + Inject the four word types into a template
  - Output
    + Sentence with no blanks

Implementation
  - Create a template
    + String
  - Variable(s) for word types
    + Array of strings
  - Variable for inputs
  - Iterate
    + Prompt the user for a different word type at each iteration
    + Store the word type
  - Interpolate the word types into a string
    + Can be done with a format string or direct interpolation
