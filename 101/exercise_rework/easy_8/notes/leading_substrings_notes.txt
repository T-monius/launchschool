# leading_substrings_notes.txt

Understanding
  - Input
    + A string
    + All alpha characters per examples
  - Task
    + Find all substrings of a given string
  - Output
    + Array
    + Increasingly longer substring elements

Implementation
  - Instantiate a return array
    + Can be a default parameter if recurring
  - Instatiate an index variable
    + Can be a default parameter in the case of recursion
  - Iterate/recurr
    + Slice a string from zero to the current index
    + PUsh the sliced string to the return array
